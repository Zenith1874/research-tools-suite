# 安居客挂牌价模块状态

> 更新：2026-07-16

## 已完成

- 全国入口真实侦察，统计局 70 城与安居客 URL 标记匹配 70/70。
- 独立本地 SQLite 表、WAL 与 30 秒 busy timeout。
- 低频会话抓取、2–4 秒间隔、最多两次退避、100 请求硬上限。
- 验证页识别与 blocked 跳过；正常空字段单独记 parse_fail。
- Nuxt 内嵌数据解析、36 个月趋势回填、价格区间护栏、原始 HTML 缓存。
- 官方二手同比主对标、分歧、散点、正负 Top10、相关系数和方向一致率。
- API、每周调度、What's New 跨库探针和 `/housing` 页面扩展。
- 完全合成的 Nuxt 结构 fixture（不含商业挂牌数据）和双库对比测试。

## 三城冒烟与定向更新

- 北京：成功，回填 36 个月；具体挂牌数值仅保留在本机独立库。
- 上海：页面正常但挂牌字段为空，记 parse_fail。
- 成都：页面正常但挂牌字段为空，记 parse_fail。
- 用户确认后定向更新北上广深、厦门、南京、合肥、常州、苏州、无锡。
- 除上海外其余 9 城成功，每城 36 个月；合计 324 条挂牌记录。
- 上海 PC/移动页面的二手全市字段均为空；没有误用移动端新房均价。
- 常州使用全国入口确认的 `cz` 子域；错误的 `changzhou` 页面因城市错配被拒绝。

## 当前状态

- 用户点名的十城已经完成定向验收；上海诚实缺失。
- 尚未发起其余统计局 70 城的全量网络请求。
- `ANJUKE_AUTO` 仍默认关闭，避免自动扩展到未点名城市；手动 API 和代码仍支持 70 城、100 请求硬上限。

## 本地输出

- 数据库：`data/housing_listing.db`（gitignored）
- 原始缓存：`data/anjuke_raw/<YYYY-MM>/`（gitignored）
- 侦察样本：`data/anjuke_raw/recon_20260716/`（gitignored）
